import {navigate} from "../../utils/navigate.js";
import Header from "../header.js";

export default function TournamentSchedulePage($container) {

    const render = () => {
        $container.innerHTML = `
             <div>TournamentSchedulePage</div>
        `;
    }

    render();
}